/*Solicite ao usuário sua idade e verifique se ele é elegível para votar nas eleições 
(idade igual ou superior a 18 anos). 
O programa deve exibir uma mensagem indicando se o usuário pode ou não votar.*/
#include <stdio.h>

int main (){
    int n;
     printf("Digite sua idade: \n");
     scanf("%d", &n);
         if ( 18 > n )

return 0;
}